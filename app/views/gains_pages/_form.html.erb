<%= form_for(@gains_page) do |f| %>
  <% if @gains_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gains_page.errors.count, "error") %> prohibited this gains_page from being saved:</h2>

      <ul>
      <% @gains_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
